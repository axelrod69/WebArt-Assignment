import 'package:flutter/material.dart';

class AppColors {
  static const shadowColor = Colors.grey;
  static const fieldColor = Colors.white;
  static const buttonColor = Colors.red;
  static const toastColor = Color.fromARGB(255, 62, 61, 61);
}